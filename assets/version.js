// version.js — sentinel polled by the shell (current.html).
// The agent overwrites this file each time it writes a new frame.html.
// Format: a single assignment of a monotonically-changing string.
// Any unique value works (ISO timestamp recommended).
window.lastVersion = "0";
